language: python

install:
    - wget https://github.com/gohugoio/hugo/releases/download/v0.53/hugo_0.53_Linux-64bit.deb
    - sudo dpkg -i hugo*.deb
    - pip install Pygments

script:
    - hugo

after_success: |
    if [ -n "$GITHUB_TOKEN" ]; then
    git checkout --orphan master
    git rm -rf .
    mv public/* .
    rm -rf public hugo_*
    echo "sanjid.me" > CNAME
    echo $TRAVIS_REPO_SLUG
    git add -f --ignore-errors --all
    git -c user.name='sanjid133' -c user.email='sanjid133@gmail.com' commit -m 'Auto Generated'
    git push -f -q https://$GITHUB_USERNAME:$GITHUB_TOKEN@github.com/$TRAVIS_REPO_SLUG master
    fi
